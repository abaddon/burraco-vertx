meta {
  name: Request deal cards
  type: http
  seq: 3
}

post {
  url: {{baseUrl}}/games/{{gameId}}/requestDealCards
  body: json
  auth: none
}

body:json {
  {
    "playerId": "{{playerId}}"
  }
}
